[package]
name = "keyboard-debouncer"
version = "0.1.0"
edition = "2021"
description = "System-level keyboard debounce daemon for faulty switches (evdev/uinput)"

[dependencies]
colored = "3.1.1"
chrono = "0.4"
evdev = "0.12"
libc = "0.2"
